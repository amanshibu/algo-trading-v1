# ============================================================
# Algo Trading Platform — LEAN Server Requirements
# ============================================================
# Only packages that the app actually imports.
# Cuts from 135 packages → ~15 (saves ~500MB+ RAM on low-spec servers)
#
# Install with: pip install -r requirements-server.txt
# ============================================================

# Core Framework
fastapi==0.128.0
uvicorn==0.40.0
python-dotenv==1.1.1
python-multipart==0.0.22
starlette==0.50.0

# Data Models
pydantic==2.12.5

# Database
SQLAlchemy==2.0.40

# ML / Data Processing (essential for the engines)
numpy==2.3.2
pandas==2.3.1
networkx==3.6.1

# Market Data
yfinance==1.1.0

# AngelOne Broker Integration
smartapi-python
pyotp

# Timezone support
pytz==2025.2
